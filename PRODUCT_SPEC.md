# Downleaf：产品需求与技术路线（Confirmed v0.4）

> 制定日期：2026-08-14（v0.4 修订：2026-08-17）<br>
> 产品范围：macOS 本地优先 Markdown 编辑器<br>
> 核心承诺：打开即写、纯键盘高效操作、长文档依然流畅、永不绑架用户文件。

## 1. 一句话定位

**一款以原生编辑性能为底座、以文件为中心、同时拥有优秀阅读体验的 Mac Markdown 编辑器。**

它不是知识库，不是项目管理工具，也不是 AI 聊天壳；它更接近“专为 Markdown 打造的 TextEdit + Reader”。

## 2. 市场空档与产品切入点

截至 2026-08-14，典型产品已经分别占据了几个明确位置：

- Typora：无缝预览、接近所见即所得。
- Obsidian：本地知识库、双链与插件生态。
- iA Writer：专注写作、语法聚焦。
- MarkEdit：轻量、开源、打开文件即可编辑。

因此，“界面极简”本身不是足够强的差异点。Downleaf 应集中做好四件事：

1. **原生编辑内核**：编辑区域不用 Web 技术，优先保证输入、选择、滚动、撤销和输入法体验。
2. **文件协作可靠**：与 Finder、Git、云盘同步目录、终端和外部 AI 工具和平共处。
3. **源码与阅读双优**：编辑区默认提供实时预览（Live Preview）——在同一份源文本上做富样式渲染，语法标记随光标按需显示；同时保留纯源码模式与精致的只读阅读模式，切换瞬时。
4. **性能是产品契约**：为启动、输入延迟、内存和大文件能力设定可测试的明确预算。

## 3. 目标用户与核心任务

### 3.1 首要用户

- 经常编辑 README、技术文档、博客草稿和会议记录的开发者。
- 使用 Finder、Git、终端、云盘或 AI 编程工具共同修改 Markdown 文件的人。
- 喜欢纯文本可迁移性，但不想忍受通用文本编辑器粗糙阅读体验的人。
- 需要稳定处理中英文混排、长文档、代码块和表格的写作者。

### 3.2 核心任务（Jobs to Be Done）

- 双击一个 `.md` 文件后立即开始写，不经过导入、建库或登录。
- 在不离开键盘的情况下完成结构化写作、查找、导航和格式调整。
- 快速在源码、分屏预览和纯阅读之间切换。
- 文件被 Git、终端或其他编辑器改动后，安全地看到最新内容且不丢失本地修改。
- 打开几万行的日志式 Markdown 或大型技术文档时，仍能顺畅输入和滚动。

## 4. 产品原则

1. **文件属于用户**：标准 Markdown 文件直接读写，不建立私有数据库。
2. **编辑优先于渲染**：任何预览、解析和统计任务都不能阻塞输入。
3. **单一事实来源**：编辑器内部永远只有 Markdown 源文本这一份数据。实时预览通过样式渲染与语法标记折叠实现（iA Writer / Obsidian 路线），不建立富文本文档模型、不做 Markdown 与富文本的双向转换，从根上避免往返失真、光标错位和输入法问题。
4. **逐步呈现复杂度**：默认只有编辑区；目录、预览、状态信息按需出现。
5. **键盘路径完整**：高频功能全部有快捷键，并可通过命令面板发现。
6. **无后台惊喜**：默认离线、无账号、无遥测、无自动上传；界面不预留头像、登录或账户状态入口。
7. **失败时保守**：解析失败不影响编辑，冲突时绝不静默覆盖内容。
8. **低频命令不常驻**：窗口内不设置全局功能图标轨；打开、最近文稿、查找、导出和偏好设置归入 macOS 系统菜单与命令面板。

## 5. MVP 需求清单

优先级定义：P0 为首个公开测试版必须具备；P1 为正式 1.0 前建议具备；P2 为后续扩展。

### 5.1 文件与文档生命周期（P0）

- 新建、打开、保存、另存为、最近文档、拖放打开、Finder 双击关联。
- 通过 Finder、拖放、`⌘O` 或明确文件路径打开现有 `.md` 时，文稿从一开始就绑定原路径，直接进入正常编辑状态，不经过导入或另存流程。
- 通过 `⌘N` 或“新建文稿”创建的内容先成为“未命名缓冲文稿”：
  - 新建时不立即弹出保存面板，焦点直接进入正文。
  - 编辑内容持续写入应用恢复缓冲区，但在用户首次保存前不擅自选择最终文件路径。
  - 用户执行 `⌘S`、`⌘W` 关闭当前文稿或退出应用时，才要求选择文件名与保存位置。
- 关闭需要确认的文稿时提供三个明确动作：
  - **保存**：未命名文稿先打开位置选择面板；已有路径文稿写回原文件，然后继续关闭或退出。
  - **不保存**：放弃本次未落盘内容并继续关闭或退出。
  - **取消**：返回编辑器，文稿与窗口保持原状。
- 设置中提供“自动保存”开关，默认开启并保存在本机：
  - 已绑定路径的文稿在输入短暂停顿后自动写回原文件。
  - 未命名缓冲文稿只自动更新恢复副本，首次落盘仍必须由用户选择位置。
  - 关闭自动保存后，已有路径但包含修改的文稿在 `⌘W` 或退出时同样显示保存确认。
- 退出应用时依次处理所有仍需确认的文稿；任何一个确认面板选择“取消”都会中止退出。
- 原生撤销/重做、自动保存、崩溃恢复与 macOS 文档版本支持。
- 原子写入，避免保存中断造成文件损坏。
- 识别 UTF-8、UTF-8 BOM、UTF-16；保存时尽量保持原编码、换行符与末尾换行状态。
- 正确处理相对路径图片与链接。
- 外部文件变更处理：
  - 当前文档无未保存修改时，自动重载并尽量保持光标与滚动位置。
  - 当前文档有未保存修改时，显示清晰的“保留本地 / 使用磁盘版本 / 比较内容”入口。
- 对云盘同步、Git checkout、终端脚本等短时间连续写入进行合并处理，避免反复闪烁。

### 5.2 编辑体验（P0）

- 原生多行文本编辑：选择、复制粘贴、拖放、拼写检查、服务菜单、语音输入和系统文本替换。
- 完整支持中文、日文、韩文输入法组合态；高亮更新不得打断正在输入的 marked text。
- Markdown 语法高亮：
  - 标题、强调、删除线、链接、图片、引用、列表、任务列表。
  - 行内代码、围栏代码块、表格、水平线、YAML front matter。
- 智能编辑行为：
  - 回车自动延续列表、任务项与引用。
  - 空列表项回车退出列表。
  - Tab / Shift-Tab 缩进或反缩进列表。
  - 快捷切换任务完成状态。
  - 包裹选中文本为粗体、斜体、代码或链接。
  - 移动行、复制行、删除行。
- 查找与替换，支持区分大小写、全词和正则表达式。
- 跳转到行、跳转到标题、返回上次编辑位置。
- 命令面板：可搜索执行全部主要命令，并展示快捷键。
- 编辑偏好：字体、字号、行距、内容宽度、段落间距、滚动越过末尾、自动换行。
- 系统浅色/深色模式；默认主题只提供一套高质量配色，避免首版主题系统膨胀。

### 5.3 Markdown 能力（P0）

- 以 CommonMark 为基础，支持常用 GitHub Flavored Markdown：
  - 表格、任务列表、删除线、自动链接、围栏代码块。
- 保留未知语法与原始 HTML，编辑器不得擅自重写用户源码。
- 阅读模式默认禁用不受信任脚本；原始 HTML 是否渲染由安全设置控制。
- 解析失败时局部降级，不影响文档继续编辑和保存。

### 5.4 阅读与预览（P0）

- 四种呈现方式：实时预览编辑（默认）、纯源码编辑、左右分屏、纯阅读。
- 实时预览编辑（Live Preview）在原生编辑器内直接完成，格式化结果本身可编辑：
  - 标题按层级放大加粗，粗体/斜体/删除线/行内代码/链接就地渲染为对应样式。
  - 语法标记（`#`、`**`、`` ` `` 等）默认视觉隐藏；光标或选区进入所在语法范围时立即恢复显示，移出后重新隐藏。
  - 引用块、无序/有序列表、任务列表渲染缩进与项目符号；任务复选框可点击切换。
  - 围栏代码块保持等宽字体与背景色块，内容永远按源码显示。
  - 首版允许表格、原始 HTML 与图片仍以源码样式呈现（P1 逐步升级为行内渲染），但不得破坏编辑。
  - 隐藏语法标记只影响显示，不改变文件内容、光标寻址、查找和复制粘贴的源文本语义。
- 模式切换不改变文件内容，不丢失选区、光标和滚动上下文。
- 精致的排版基线：正文宽度、行长、标题层级、表格、引用、代码块、图片和链接状态。
- 阅读模式支持文本选择、复制、系统查找和打印。
- 编辑与预览按块同步滚动；首版不追求逐像素同步。
- `Command` 点击链接时明确区分：
  - 文档内锚点。
  - 相对文件链接。
  - 外部网页链接。
- 图片加载失败时显示可读的占位和原路径。

### 5.5 导航与信息（P0）

- 提供常驻于窗口右侧、可隐藏的快速大纲，内容来自 Markdown 的 H1–H6 标题。
- 大纲按标题层级缩进并支持逐级折叠；遇到跳级标题时挂到最近的上级标题下。
- 点击标题立即跳转：编辑模式把光标放到标题行，阅读模式滚动到对应锚点，分屏模式同步两侧位置。
- 根据编辑光标或阅读滚动位置高亮当前章节，并自动确保当前条目处于大纲可视范围内。
- 支持完整键盘导航：上下选择、左右折叠/展开、Return 跳转、Home/End 到首尾。
- 大纲宽度可直接拖动分隔线调整，关闭窗口或重启应用后保持用户上次设置。
- 长标题单行截断，悬停显示完整内容；大纲为空时显示轻量空状态。
- 状态信息默认弱化显示：字数、字符数、行数、选区统计、光标行列。
- 窗口标题显示文件名与未保存状态，路径可从标题菜单访问。
- 最近文档和系统“打开最近使用”行为保持一致。

### 5.6 无障碍与系统融合（P0）

- VoiceOver 可读出编辑区、按钮、模式与文档状态。
- 完整键盘焦点顺序，支持“减少动态效果”和“增强对比度”。
- 使用系统标准菜单、窗口、快捷键和服务。
- 支持 Quick Look/Spotlight 的能力放入 P1，不阻塞 MVP。

## 6. P1 与 P2 能力

### P1：1.0 完整体验

- 可选文件夹侧栏与模糊快速打开；尊重隐藏文件和 `.gitignore` 设置。
- 粘贴或拖入图片时复制到可配置资源目录，并插入相对链接。
- 导出 HTML、PDF；打印样式与阅读主题一致。
- Quick Look 扩展与 Spotlight 元数据支持。
- 自定义阅读 CSS，但不开放执行任意脚本。
- 脚注、代码语法着色、数学公式、Mermaid；均按需加载。
- 多窗口标签页与恢复上次窗口布局。
- 外部变更的可视化差异比较。

### P2：验证需求后再做

- 命令行工具：从终端打开、跳转到行、等待文件关闭。
- Shortcuts / App Intents 集成。
- 可配置文本动作与只读预览服务器。
- Git 变更标记或审阅模式。
- 有严格权限模型的扩展机制。

## 7. 明确不做的事情

首版不做：

- 知识库、双链、关系图谱、标签数据库。
- 用户账号、登录/注册、个人资料、会员订阅、云同步、多人实时协作；P0/P1 不预留相关界面或后台骨架。
- 插件市场和完整主题市场。
- 内置 AI 聊天窗口或自动改写正文。
- 跨平台、iOS 伴侣应用。
- 全量文档模型式所见即所得（Typora 路线：Markdown ↔ 富文本双向转换）；实时预览只做同一份源文本的样式渲染与标记折叠。
- 自创 Markdown 方言或保存时自动格式化整篇文档。

这些边界的目的不是永久排除，而是保护首版的可靠性、速度和可理解性。

## 8. 关键交互设计

### 8.1 默认窗口

- 打开文件后焦点直接进入正文。
- `⌘N` 与“新建文稿”直接创建未命名缓冲文稿，不用先回答文件名或保存位置；标题栏明确显示“暂存于缓冲区”。
- 未命名缓冲文稿首次执行 `⌘S` 时选择保存位置；执行 `⌘W` 或退出应用时先显示“保存 / 不保存 / 取消”确认。
- 自动保存默认开启，但只对已有路径的文稿直接写回；它不会替用户为未命名文稿决定最终位置。
- 不设置最左侧全局功能图标轨；可选文稿栏直接成为窗口内容的最左侧，收起后正文自然扩展。
- 不显示头像、登录按钮、账户菜单或“离线账户”状态；本地文件属性只在与当前操作直接相关时出现。
- 顶部只保留当前阅读方式与大纲显示控制；搜索、打开、最近文稿、导出和设置进入 macOS 菜单栏与命令面板。
- 首版菜单遵循 Mac 习惯组织为“文件 / 编辑 / 视图 / 窗口 / 帮助”，不在窗口内重复一套导航系统。
- 右侧大纲首次默认展开，之后记住用户的显示状态和宽度；视觉层级弱于正文。
- 不显示欢迎仪表盘；无文档时仅提供“新建”和“打开”。

### 8.2 快捷键建议

| 操作 | 快捷键 |
| --- | --- |
| 新建缓冲文稿 | `⌘N` |
| 打开现有文件 | `⌘O` |
| 保存 / 首次选择位置 | `⌘S` |
| 关闭当前文稿 | `⌘W` |
| 命令面板 | `⌘K` |
| 阅读 / 编辑切换 | `⌘E` |
| 实时预览 / 源码切换 | `⇧⌘E` |
| 分屏预览 | `⌥⌘E` |
| 大纲显示 / 隐藏 | `⌥⌘O` |
| 跳转标题 | `⌘J` |
| 切换任务状态 | `⌘Return` |
| 粗体 / 斜体 / 行内代码 | `⌘B` / `⌘I` / <kbd>⌘</kbd> + <kbd>`</kbd> |

快捷键必须最终通过 macOS 现有惯例冲突检查，并允许用户在系统键盘设置中覆盖。

### 8.3 右侧快速大纲

- **位置**：始终位于内容区最右侧。P1 的文件夹侧栏位于左侧，两者职责不混合。
- **布局关系**：
  - 编辑模式：正文 + 大纲。
  - 分屏模式：源码 + 预览 + 大纲。
  - 阅读模式：阅读内容 + 大纲。
- **尺寸规则**：默认宽度 260 pt，最小 180 pt，最大为 480 pt 或窗口宽度的 40%，取较小值。
- **拖拽行为**：分隔线视觉宽度保持 1 px，但提供约 8 pt 的鼠标命中区域；悬停显示左右调整光标，拖动时实时改变宽度。
- **快捷恢复**：双击分隔线恢复 260 pt；拖至最小宽度以下时折叠，通过工具栏或 `⌥⌘O` 重新展开。
- **状态保持**：宽度、显示/隐藏状态和折叠层级保存到本地窗口恢复状态，不写入 Markdown 文件。
- **当前位置跟随**：
  - 编辑时，以光标所在标题区间作为当前章节。
  - 阅读时，以视口顶部以下约 25% 位置对应的章节作为当前章节，减少临界点闪烁。
  - 用户主动滚动大纲时，短暂暂停自动跟随，避免列表被强行拉回。
- **更新策略**：标题文本或层级改变后异步更新，并对树节点做差量更新；不得重置大纲滚动位置或抢走编辑焦点。
- **窄窗口降级**：当正文可用宽度将低于 520 pt 时优先自动折叠大纲，但保留明确的重新展开入口。
- **明确不做**：P0 不支持在大纲中拖动标题来重排正文，避免误改长文档结构。

### 8.4 大文件安全模式

- 文档超过可配置阈值（初始建议 10 MB 或 100,000 行）时自动进入安全模式。
- 暂停实时预览、全文统计和复杂语法扩展，只保留编辑、搜索、保存、可见区域高亮和轻量标题索引。
- 明确提示被暂停的能力，并允许用户手动恢复。

## 9. 推荐技术路线

### 9.1 平台与语言

- 平台：macOS 14+，Apple Silicon 优先，同时保留 Intel 编译兼容直到数据证明可以移除。
- 语言：Swift 6，开启严格并发检查。
- 工程：使用标准 Xcode macOS App target 管理运行、调试、签名和归档；`Package.swift` 仅保留轻量测试与模块化构建能力。
- UI：**AppKit 负责应用、窗口、菜单、文档生命周期与核心编辑器；SwiftUI 只用于设置页和少量独立组件。**
- 架构：模块化单体，不在 MVP 阶段引入微服务、本地数据库或复杂依赖注入框架。

选择原生路线的原因：产品只面向 Mac，核心竞争力正是文本输入、系统融合和可预测性能；Electron、Monaco 或 CodeMirror 会削弱这项差异化。

### 9.2 文档架构

- 使用成熟的 `NSDocument` / `NSDocumentController` 管理打开、保存、自动保存、版本和关闭确认。
- 已有文件以带 `fileURL` 的 `NSDocument` 打开；未命名文稿保持 `fileURL == nil`，直到用户首次确认保存位置。
- 对已有路径文稿启用原位自动保存，并以短延迟合并连续输入；对未命名文稿只写应用管理的恢复快照，不触发静默“另存为”。
- `NSDocumentController` 统一编排 `⌘W` 与应用退出流程，确保多个未保存文稿逐个确认，任意“取消”都能终止退出。
- 使用 `NSFilePresenter` 观察外部变更，并在独立串行协调器中合并短时间连续事件。
- 文件读取、编码检测、解析和差异计算放到后台任务；UI 状态只在主线程提交。
- 文件夹权限使用 security-scoped bookmarks；MVP 单文件打开不需要建立工作区数据库。

不建议把首版建立在仅新系统可用的 SwiftUI 新文档 API 上，否则会无必要地压缩系统覆盖面。

### 9.3 编辑器内核

- 自建薄层 `MarkdownTextView`，继承并扩展启用 TextKit 2 的 `NSTextView`；不从零重做文本输入、选择和渲染控件。
- 通过 `NSTextView` 已有的 TextKit 2 组件扩展行为：
  - `NSTextContentStorage`
  - `NSTextLayoutManager`
  - `NSTextViewportLayoutController`
- 保留框架文本视图免费提供的输入、选择、无障碍、撤销/重做、听写、拼写检查和系统文本服务。
- 只对变更行和可见区域执行语法着色；异步结果带文档版本号，过期结果直接丢弃。
- 高亮器使用轻量的“按行状态机”，服务于编辑显示，不承担 Markdown 正确性判定。
- 语义解析器独立运行，负责大纲、预览、链接和导出。
- 所有自动补全行为以原始文本编辑事务实现，确保撤销一次即可还原。

可参考 STTextView 的 TextKit 2 实践，但它采用 GPL-3.0 或商业许可，不建议在未确认产品开源/商业模式前作为硬依赖。

### 9.3.1 实时预览（Live Preview）实现

技术路线采用业界原生 Markdown 编辑器的主流做法（iA Writer、Bear、Ulysses 同路线）：**在 TextKit 2 上对同一份源文本做富样式渲染与语法标记折叠**，不引入 WebView 编辑器，也不建立第二份文档模型。

- **样式渲染**：在按行状态机识别出语法范围后，通过 `NSTextContentStorage` / `NSTextLayoutManager` 为对应 range 施加渲染属性（字体、字号、颜色、缩进、背景色块）。属性只影响显示层，永不写回源文本。
- **标记折叠**：语法标记（`#`、`**`、`` ` ``、`>` 等）通过 TextKit 2 的布局层隐藏——首选为标记 range 设置零宽呈现（自定义 `NSTextLayoutFragment` / 渲染属性），保持“显示位置”与“源文本偏移”的映射由布局层统一负责，避免手工维护两套坐标。
- **光标感知**：监听选区变化，计算光标/选区命中的语法元素范围；命中范围内的标记恢复可见并参与正常编辑，移出后重新折叠。该计算按行做增量，不做全文重排。
- **编辑语义不变**：复制、查找、字数统计、大纲偏移一律基于源文本；折叠标记不改变 `NSRange` 寻址。撤销/重做、输入法 marked text、听写和拼写检查全部沿用 `NSTextView` 原生实现，折叠逻辑不得介入输入事务。
- **增量更新**：只对变更行与可见视口重算样式与折叠；异步结果带文档版本号，过期结果丢弃（与语法着色共用同一失效机制）。
- **降级路径**：大文件安全模式下自动退回纯源码样式；`⇧⌘E` 可随时手动切换，作为任何折叠渲染缺陷的逃生通道。
- **分阶段范围**：第一阶段覆盖标题、粗体/斜体/删除线、行内代码、链接、引用、列表与任务列表；表格、图片行内渲染与脚注放入 P1，按需求逐块推进，不追求一次到位。

### 9.4 Markdown 解析与渲染

- MVP 采用零外部依赖的轻量解析层：逐行状态机负责 H1–H6 大纲与常用 Markdown 阅读渲染，优先保证启动速度、可控内存和离线构建。
- 用 CommonMark/GFM fixture 持续校验兼容性；当脚注、复杂嵌套、完整 GFM 或导出能力进入范围时，再统一切换到 `swift-markdown`（底层由 `cmark-gfm` 驱动），不同时维护两个“权威解析器”。
- 输入停止约 120 ms 后启动可取消解析；连续输入时旧任务立即作废。
- 只有预览可见时才构建渲染结果。
- 阅读模式（只读）继续使用惰性创建的 `WKWebView` 加载本地生成 HTML；实时预览编辑完全在原生 TextKit 2 内实现，二者互不依赖：
  - 编辑器本身仍为完全原生文本控件。
  - 禁止网络请求和任意 JavaScript。
  - 使用 Content Security Policy。
  - 通过块 ID 实现标题/段落级滚动同步。
- 关闭预览后释放或休眠渲染实例，避免长期占用内存。

### 9.5 大纲实现

- 使用 AppKit `NSSplitViewController` 管理内容区与右侧大纲，精确控制最小/最大宽度、折叠和拖拽行为。
- 使用 `NSSplitView.autosaveName` 与窗口恢复状态保存分隔位置；业务层只保存显示偏好，不自行记录每次拖拽事件。
- 大纲视图使用虚拟化的 `NSOutlineView`；MVP 对轻量标题树做保留选中与折叠状态的更新，达到性能阈值后再引入 diffable data source。
- `OutlineItem` 至少包含稳定 ID、标题层级、纯文本标题、源码范围和预览锚点。
- 标题索引优先来自 `MarkdownSemantic` 的 AST；大文件安全模式下改用轻量逐行标题扫描，保证大纲仍然可用。
- 当前章节计算与大纲滚动分别节流；只在章节真正变化时更新选中项，不随每个字符或每个滚动像素刷新。
- 分隔线、折叠按钮和树节点都必须暴露 VoiceOver 标签；分隔线可通过键盘逐级调整宽度。

### 9.6 建议模块边界

```text
DownleafApp
├── AppShell            窗口、菜单、命令、设置
├── DocumentCore        读写、编码、自动保存、外部变更
├── EditorCore          TextKit 2、选择、撤销、输入法、搜索
├── MarkdownSyntax      增量着色、实时预览样式与标记折叠、智能编辑行为
├── MarkdownSemantic    AST、大纲、链接、统计
├── Outline             标题索引、树状态、当前章节与快速跳转
├── Reader              HTML 渲染、样式、滚动同步、打印
├── Workspace           文件夹与快速打开（P1）
└── TestSupport         文档语料、性能基准、故障注入
```

模块间传递不可变快照或小型事件，避免把整个文本存储复制给多个观察者。

## 10. 性能预算与验收目标

以下数字是产品目标，需要在首轮技术验证中用真实设备校准：

| 场景 | 目标 |
| --- | --- |
| Apple Silicon 冷启动至可输入 | P95 ≤ 400 ms |
| 打开 1 MB Markdown 至可输入 | P95 ≤ 250 ms |
| 打开 10 MB Markdown 至可输入 | P95 ≤ 1 s |
| 持续输入时主线程单次工作 | P95 < 8 ms |
| 普通文档滚动 | 稳定 60 fps；高刷设备尽量达到 120 fps |
| 空闲 CPU | < 0.5% |
| 空文档常驻内存 | ≤ 70 MB |
| 10 MB 文档编辑模式内存 | ≤ 150 MB |
| 自动保存造成的可感知停顿 | 不得超过一帧 |
| 点击大纲至正文完成定位 | P95 ≤ 50 ms |
| 10,000 个标题的大纲滚动与折叠 | 稳定 60 fps |

性能验收必须覆盖：

- 纯英文、中文长文、中英混排、Emoji、超长单行。
- 10,000 个标题、深层列表、大表格、大代码块、大量链接。
- 输入法组合、快速撤销重做、全选替换、持续滚动。
- 预览开启和关闭两种状态。
- 外部程序高频改写文件。
- 10,000 个标题的大纲构建、折叠、键盘导航和当前位置跟随。

## 11. 质量与安全验收

### P0 发布阻断条件

- 任意已知操作可能静默丢失用户内容。
- 新建文稿在首次保存前没有可靠恢复缓冲，或应用替用户静默决定最终保存位置。
- `⌘W` / 退出应用未覆盖“保存 / 不保存 / 取消”，或“取消”后仍继续关闭。
- 中文/日文输入法导致字符重复、丢失、光标跳动或撤销错乱。
- 外部变更会直接覆盖未保存内容。
- 解析或图片加载失败导致编辑器崩溃。
- 关闭窗口、休眠或系统重启后无法恢复应恢复的未保存文档。
- 普通 Markdown 文件被保存后发生非用户意图的格式重写。

### 测试体系

- 单元测试：智能编辑、编码、换行、链接解析、大纲生成。
- Golden tests：Markdown 输入到 AST/HTML 的固定输出。
- Round-trip tests：读取并原样保存时，字节级保持或只有明确允许的编码差异。
- UI tests：快捷键、菜单、模式切换、恢复、冲突流程。
- 文稿生命周期 tests：路径打开、`⌘N` 缓冲、首次保存位置、自动保存开关、`⌘W` 三分支与多文稿退出中止。
- 大纲 tests：标题跳级、重名标题、标题改名、当前位置跟随、宽度恢复和窄窗口折叠。
- 性能测试：固定语料库与 Instruments signpost 指标，提交前检测回退。
- 故障测试：模拟磁盘满、权限变化、文件删除、外部覆盖与损坏图片。

## 12. 开发阶段建议

### Phase 0：技术验证（2–3 天）

- TextKit 2 编辑 1 MB / 10 MB 文件的启动、输入、滚动与内存基线。
- 中文和日文输入法、撤销栈、可见区域高亮实验。
- `NSDocument` 自动保存与外部变更冲突实验。
- 轻量解析层的 GFM fixture 通过率、大文档解析耗时与降级边界；只有兼容性缺口达到切换阈值时才评估 `swift-markdown`。
- 惰性预览和滚动同步原型。

**退出条件**：性能预算可行，核心风险有明确解决路径，再进入界面实现。

### Phase 1：可用编辑器（1–2 周）

- 文档生命周期、编辑内核、基础高亮、搜索、设置。
- 建立性能测试语料库和自动化基准。

### Phase 2：Markdown 工作流（1–2 周）

- 智能列表、任务项、命令面板、大纲、阅读与分屏模式。

### Phase 3：可靠性与系统融合（1 周）

- 外部变更、冲突处理、崩溃恢复、无障碍、编码边界。

### Phase 4：发布打磨（1 周）

- 性能剖析、大文件安全模式、签名、公证、更新机制与测试版反馈。

单人开发的高质量 MVP 估计为 5–7 周；该估计应在 Phase 0 完成后重算。

## 13. 首轮成功指标

- 5 秒内完成“打开已有文件并开始输入”。
- 核心任务不依赖鼠标，也不要求学习产品专有概念。
- 测试用户连续使用一周后，未发生内容丢失或冲突误覆盖。
- 1 MB 技术文档中输入、滚动与模式切换没有明显卡顿。
- 至少 70% 测试用户认为阅读模式可替代浏览器打开本地 Markdown。
- 测试用户能够在不看说明的情况下发现命令面板、大纲和预览切换。

## 14. 当前需要冻结的产品决策

建议立即冻结：

- 文件优先、本地优先、无账号。
- 新建文稿先进入恢复缓冲区，首次保存、关闭或退出时再由用户决定最终位置；自动保存默认开启，但不替未命名文稿选择路径。
- 源码编辑为默认模式；阅读与分屏按需开启。
- 原生 AppKit/TextKit 2 编辑内核；AppKit 统一负责应用与窗口外壳，SwiftUI 只负责设置页和少量独立组件。
- MVP 使用零外部依赖的轻量 Markdown 解析层；完整 GFM、导出或复杂扩展进入范围时再统一评估语义 AST。
- MVP 不做知识库、插件市场、内置 AI 和全量所见即所得。
- 性能指标和内容安全问题具备发布阻断权。

Phase 0 后再决定：

- 最低系统版本最终定为 macOS 14 还是 macOS 15。
- 阅读模式采用 `WKWebView` 还是纯 TextKit 渲染的长期路线。
- Intel 支持是否进入 1.0。

## 15. 调研参考

- [Apple: TextKit 2](https://developer.apple.com/videos/play/wwdc2021/10061/)
- [Apple: Elevate your app’s text experience with TextKit (WWDC26)](https://developer.apple.com/videos/play/wwdc2026/370/)
- [Apple: Building a text editor with SwiftUI](https://developer.apple.com/videos/play/wwdc2022/10089/)
- [Apple: Document-Based Apps Programming Guide](https://developer.apple.com/library/archive/documentation/DataManagement/Conceptual/DocBasedAppProgrammingGuideForOSX/Introduction/Introduction.html)
- [Swift Markdown](https://github.com/swiftlang/swift-markdown)
- [cmark-gfm](https://github.com/github/cmark-gfm)
- [STTextView](https://github.com/krzyzanowskim/STTextView)
- [Typora](https://typora.io/)
- [Obsidian](https://obsidian.md/)
- [iA Writer](https://ia.net/writer)
- [MarkEdit](https://markedit.app/)
